# Gradle
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# Plugin
pluginGroup=com.example.airefactoring
pluginName=AI Refactoring MVP
pluginVersion=0.1.0
pluginSinceBuild=261
pluginUntilBuild=261.*

# IntelliJ Platform
platformType=IC
platformVersion=2026.1

# Toolchain
# javaVersion is the single source of truth for the JDK (build.gradle.kts reads it).
# NOTE: IntelliJ plugins run on the IDE's bundled JetBrains Runtime. If the target
# IDE's runtime or the Kotlin compiler rejects 25, lower this to 21 — that is the
# only change required.
javaVersion=25
kotlinVersion=2.2.0
